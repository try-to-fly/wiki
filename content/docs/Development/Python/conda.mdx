# Conda

python 虚拟环境

### 使用

1. 创建虚拟环境

```bash
conda create --name xxx python=3.11
```

### 配置

1. 避免显示通知:`export CONDA_NUMBER_CHANNEL_NOTICES=0`

### 踩过的坑

1. `conda`创建环境的时候，需要指定 python 版本，不然依赖会不正确