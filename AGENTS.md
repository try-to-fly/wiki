# AGENTS.md

本仓库的完整 Agent 工作说明请看：`AGENT.md`。

最小约定（供工具快速读取）：

- 交流语言：中文（简体）；英文技术术语保留原文，必要时补充中文解释
- Node 依赖：优先使用 `pnpm`（锁文件为 `pnpm-lock.yaml`）
- 常用命令：`pnpm dev`（端口 8888）、`pnpm build`
- 内容位置：主要文档在 `pages/docs/**`；导航/侧边栏由 `_meta.json` 管理

